# In-Memory Task CRUD API

A lightweight REST API built with FastAPI implementing full CRUD operations on an in-memory task store.

## How to Run

1. Install dependencies:
   ```bash
   pip install fastapi uvicorn