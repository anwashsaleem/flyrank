# SQLite Task CRUD API

Persistent REST API using FastAPI and SQLite for database-backed task management.

## Setup & Run

1. Start server:
   ```bash
   uvicorn backend-track.a2.main:app --reload --port 8000